name: Tests

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ v0.2.x ]

jobs:
  run-tests:
    name: Run all tests
    uses: ./.github/workflows/run_cargo.yaml
    with:
      cargo-command: cargo test --verbose